{
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "move",
        "turn"
      ]
    },
    "parameters": {
      "type": "object",
      "properties": {
        "direction": {
          "type": "string",
          "enum": [
            "forward",
            "backward",
            "left",
            "right",
            "straight"
          ],
          "default": "straight"
        },
        "distance": {
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "x": {
                  "type": "number"
                },
                "y": {
                  "type": "number"
                },
                "z": {
                  "type": "number"
                }
              },
              "required": [
                "x",
                "y",
                "z"
              ],
              "description": "3D movement vector components in meters."
            },
            {
              "type": "string",
              "enum": [
                "move"
              ],
              "description": "Simple move command."
            }
          ]
        },
        "angle_rad": {
          "type": "number",
          "description": "Rotation angle in radians for turning."
        }
      },
      "oneOf": [
        {
          "title": "Move action parameters",
          "required": [
            "distance"
          ]
        },
        {
          "title": "Turn action parameters",
          "required": [
            "angle_rad"
          ]
        }
      ]
    }
  },
  "required": [
    "action",
    "parameters"
  ]
}


