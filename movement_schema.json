{
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "move",
        "turn"
      ]
    },
    "parameters": {
      "type": "object",
      "properties": {
        "direction": {
          "type": "string",
          "enum": [
            "forward",
            "backward",
            "left",
            "right",
            "straight"
          ],
          "default": "straight"
        },
        "distance": {
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "x": {
                  "type": "number"
                },
                "y": {
                  "type": "number"
                },
                "z": {
                  "type": "number"
                }
              },
              "required": [
                "x",
                "y",
                "z"
              ],
              "description": "Absolute 3D position components in meters."
            },
            {
              "type": "object",
              "properties": {
                "dx": {
                  "type": "number"
                },
                "dy": {
                  "type": "number"
                },
                "dz": {
                  "type": "number"
                }
              },
              "required": [
                "dx",
                "dy",
                "dz"
              ],
              "description": "Relative 3D translation components in meters."
            },
            {
              "type": "string",
              "enum": [
                "move"
              ],
              "description": "Simple move command."
            }
          ]
        },
        "angle_rad": {
          "type": "number",
          "description": "Rotation angle in radians for turning."
        }
      },
      "oneOf": [
        {
          "title": "Move action parameters",
          "required": [
            "distance"
          ]
        },
        {
          "title": "Turn action parameters",
          "required": [
            "angle_rad"
          ]
        }
      ]
    }
  },
  "required": [
    "action",
    "parameters"
  ]
}


